# AiSnakeGameModel

A reinforcement learning project that trains an AI agent to autonomously play Snake, achieving progressively higher scores over training episodes. The model implements a Deep Q-Network (DQN) algorithm utilizing an experience replay buffer and an epsilon-greedy exploration strategy. It relies on a custom reward shaping system to guide the agent toward food while actively avoiding collisions, complete with visualizers to track measurable performance improvements during training.
